fix: 修复 JS 模板字面量导致的全局脚本崩溃

模板字面量改用反引号 + ${} 内插，彻底避免
之前 \ 行续符 + ' 混用时字符串未闭合的问题。

这是之前多次部署后页面白屏/无法切换 Tab 的根本原因：
el.innerHTML='<div>\\  ← 行续符让字符串一直开着
'+expr+'...          ← 本应闭合字符串的 ' 被吞进字符串内
→ 整个 JS 解析失败 → 页面无数据、Tab 不工作
